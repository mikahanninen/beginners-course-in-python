tasks:
  downloadTask:
    command:
      - python
      - downloader.py
  processingTask:
    command:
      - python
      - processing.py

condaConfigFile: conda.yaml
artifactsDir: output
PATH:
  - .
PYTHONPATH:
  - .
ignoreFiles:
  - .gitignore
